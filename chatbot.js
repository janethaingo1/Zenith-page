/** * Premium AI Chatbot Logic * (c) 2024 Zenith Private Wealth - Senior Frontend & AI Implementation */ // API Parameters const API_URL = 'https://9router.vuhai.io.vn/v1'; const API_KEY = 'sk-4bd27113b7dc78d1-lh6jld-f4f9c69f'; const MODEL_ID = 'ces-chatbot-gpt-5.4'; let KnowledgeBase = ""; let MessagesMemory = []; /** * Initialize Knowledge Base from File */ async function loadKnowledgeBase() { try { const response = await fetch('chatbot_data.txt'); if (!response.ok) throw new Error("Could not load knowledge base file."); KnowledgeBase = await response.text(); initializeSystemRole(); } catch (err) { console.warn("Knowledge base load error, using default fallback.", err); KnowledgeBase = "Tên chuyên gia: Nguyễn Văn A. Định vị: Chuyên gia AI & Tự động hóa. Liên hệ: 0123456789"; initializeSystemRole(); } } function initializeSystemRole() { const systemPrompt = { role: "system", content: `Bạn là trợ lý AI độc quyền cho chuyên gia Nguyễn Văn A. Dưới đây là kiến thức cơ sở (Knowledge Base) của chuyên gia: ${KnowledgeBase} QUY TẮC PHẢN HỒI: 1. Bạn CHỈ được phép trả lời dựa trên nội dung trong Knowledge Base trên. 2. Mọi câu trả lời PHẢI sử dụng định dạng Markdown đẹp mắt (headings, lists, bold text, etc.). 3. LUÔN chào hỏi thân thiện ở đầu hội thoại. 4. LUÔN trả lời rõ ràng, mạch lạc, đi thẳng vào vấn đề. 5. LUÔN kết thúc bằng một lời mời hỏi thêm về giải pháp AI hoặc Automation. 6. Nếu người dùng hỏi ngoài phạm vi kiến thức được cung cấp: Hãy từ chối nhẹ nhàng, giải thích rằng bạn là trợ lý chuyên sâu về mảng của anh A, sau đó hướng dẫn người dùng liên hệ qua Email/Zalo có trong Knowledge Base.` }; MessagesMemory = [systemPrompt]; } /** * DOM Constants */ const chatToggle = document.getElementById('chat-toggle'); const chatWindow = document.getElementById('chat-window'); const chatClose = document.getElementById('chat-close'); const chatRefresh = document.getElementById('chat-refresh'); const chatMessages = document.getElementById('chat-messages'); const chatInput = document.getElementById('chat-input'); const chatSend = document.getElementById('chat-send'); const DEFAULT_GREETING = "Xin chào! Tôi là trợ lý AI của **chuyên gia Nguyễn Văn A**. Tôi có thể giúp gì cho bạn về các giải pháp AI, N8N hay khóa học Agentic AI?"; /** * Event Listeners */ chatToggle.addEventListener('click', () => { chatWindow.classList.toggle('hidden'); if (!chatWindow.classList.contains('hidden') && chatMessages.children.length === 0) { appendMessage(DEFAULT_GREETING, 'ai'); } chatInput.focus(); }); chatClose.addEventListener('click', () => { chatWindow.classList.add('hidden'); }); // Refresh Logic Requirement chatRefresh.addEventListener('click', async () => { const icon = chatRefresh.querySelector('.material-symbols-outlined'); // 1. Start spinning animation icon.classList.add('refresh-spin'); // 2. Clear all chat history UI chatMessages.innerHTML = ''; // 3. Reset logic memory initializeSystemRole(); // 4. Display default greeting setTimeout(() => { appendMessage(DEFAULT_GREETING, 'ai'); }, 100); // 5. Stop animation after exactly 500ms setTimeout(() => { icon.classList.remove('refresh-spin'); }, 500); }); chatInput.addEventListener('keydown', (e) => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); handleSendMessage(); } }); chatSend.addEventListener('click', handleSendMessage); /** * Messaging Core Logic */ async function handleSendMessage() { const userText = chatInput.value.trim(); if (!userText) return; // Reset input chatInput.value = ''; chatInput.style.height = 'auto'; // Reset height // User UI update appendMessage(userText, 'user'); // Typing Animation UI const typingElementId = renderTypingIndicator(); try { // Prepare context MessagesMemory.push({ role: "user", content: userText }); // API Call const response = await fetch(`${API_URL}/chat/completions`, { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${API_KEY}` }, body: JSON.stringify({ model: MODEL_ID, messages: MessagesMemory, temperature: 0.7 }) }); if (!response.ok) throw new Error("API call failed"); const data = await response.json(); const aiMessage = data.choices[0].message.content; // Save AI response to history MessagesMemory.push({ role: "assistant", content: aiMessage }); // Remove typing → Show AI response removeTypingIndicator(typingElementId); appendMessage(aiMessage, 'ai'); } catch (error) { console.error("AI Communication Error:", error); removeTypingIndicator(typingElementId); appendMessage("Rất tiếc, hệ thống đang gặp sự cố kết nối. Bạn vui lòng thử lại sau hoặc liên hệ trực tiếp với anh A qua Zalo nhé!", 'ai'); } } /** * UI Rendering Helpers */ function appendMessage(text, role) { const wrapper = document.createElement('div'); wrapper.className = `flex ${role === 'user' ? 'justify-end' : 'justify-start'} mb-4 fade-in-up`; const bubble = document.createElement('div'); bubble.className = `max-w-[85%] px-4 py-3 ${role === 'user' ? 'chat-bubble-user text-white' : 'chat-bubble-ai'}`; if (role === 'ai') { const container = document.createElement('div'); container.className = 'chat-markdown'; // USE MARKED.JS AS REQUIRED container.innerHTML = marked.parse(text); bubble.appendChild(container); } else { bubble.textContent = text; bubble.className += ' text-sm leading-normal'; } wrapper.appendChild(bubble); chatMessages.appendChild(wrapper); // Auto Scroll Down scrollChatToBottom(); } function renderTypingIndicator() { const id = 'typing-' + Date.now(); const wrapper = document.createElement('div'); wrapper.id = id; wrapper.className = 'flex justify-start mb-4'; wrapper.innerHTML = ` <div class="chat-bubble-ai px-4 py-3 flex items-center space-x-2"> <span class="text-[10px] text-gray-400 font-bold uppercase tracking-widest">Đang nhập</span> <div class="typing-indicator"> <div class="typing-dot"></div> <div class="typing-dot"></div> <div class="typing-dot"></div> </div> </div> `; chatMessages.appendChild(wrapper); scrollChatToBottom(); return id; } function removeTypingIndicator(id) { const el = document.getElementById(id); if (el) el.remove(); } function scrollChatToBottom() { chatMessages.scrollTo({ top: chatMessages.scrollHeight, behavior: 'smooth' }); } // Auto-expand textarea chatInput.addEventListener('input', function() { this.style.height = 'auto'; this.style.height = (this.scrollHeight) + 'px'; if (this.scrollHeight > 120) { this.style.overflowY = 'auto'; } else { this.style.overflowY = 'hidden'; } }); // Run loadKnowledgeBase();`;
